---
ms.date: 07/10/2019
keywords: jea, powershell, segurança
title: Funcionalidades de função JEA
ms.openlocfilehash: 7191b90e198ffb539da6870a8ddc3e449ad9e8ae
ms.sourcegitcommit: 46bebe692689ebedfe65ff2c828fe666b443198d
ms.translationtype: MT
ms.contentlocale: pt-PT
ms.lasthandoff: 07/10/2019
ms.locfileid: "67726601"
---
# <a name="jea-role-capabilities"></a><span data-ttu-id="a9cf4-103">Funcionalidades de função JEA</span><span class="sxs-lookup"><span data-stu-id="a9cf4-103">JEA Role Capabilities</span></span>

<span data-ttu-id="a9cf4-104">Ao criar um ponto final JEA, terá de definir uma ou mais funcionalidades de função que descrevem o que alguém pode fazer numa sessão JEA.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-104">When creating a JEA endpoint, you need to define one or more role capabilities that describe what someone can do in a JEA session.</span></span> <span data-ttu-id="a9cf4-105">Um recurso de função é um arquivo de dados do PowerShell com o `.psrc` extensão que apresenta uma lista de todos os cmdlets, funções, fornecedores e programas externos que ficam disponíveis para os utilizadores a ligar.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-105">A role capability is a PowerShell data file with the `.psrc` extension that lists all the cmdlets, functions, providers, and external programs that are made available to connecting users.</span></span>

## <a name="determine-which-commands-to-allow"></a><span data-ttu-id="a9cf4-106">Determinar quais comandos para permitir</span><span class="sxs-lookup"><span data-stu-id="a9cf4-106">Determine which commands to allow</span></span>

<span data-ttu-id="a9cf4-107">A primeira etapa na criação de um arquivo de recurso de função é considerar o que os utilizadores precisam de acesso a.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-107">The first step in creating a role capability file is to consider what the users need access to.</span></span> <span data-ttu-id="a9cf4-108">Os requisitos de processo de recolha podem demorar algum tempo, mas é um processo importante.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-108">The requirements gathering process can take a while, but it's an important process.</span></span> <span data-ttu-id="a9cf4-109">Fornecer acesso de utilizadores à muito poucos cmdlets e funções pode impedi-los de seu trabalho.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-109">Giving users access to too few cmdlets and functions can prevent them from getting their job done.</span></span> <span data-ttu-id="a9cf4-110">Permitir o acesso a muitos cmdlets e funções, pode permitir que os usuários façam mais do que a finalidade e enfraquecer a sua postura de segurança.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-110">Allowing access to too many cmdlets and functions can allow users to do more than you intended and weaken your security stance.</span></span>

<span data-ttu-id="a9cf4-111">Como para este processo depende da sua organização e objetivos.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-111">How you go about this process depends on your organization and goals.</span></span> <span data-ttu-id="a9cf4-112">As sugestões seguintes podem ajudar a garantir que está no caminho certo.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-112">The following tips can help ensure you're on the right path.</span></span>

1. <span data-ttu-id="a9cf4-113">**Identificar** os utilizadores de comandos estiver a utilizar para realizar seu trabalho.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-113">**Identify** the commands users are using to get their jobs done.</span></span> <span data-ttu-id="a9cf4-114">Isso pode envolver a apurar o departamento de TI, a verificação de scripts de automatização ou analisar os registos e transcrições de sessão do PowerShell.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-114">This may involve surveying IT staff, checking automation scripts, or analyzing PowerShell session transcripts and logs.</span></span>
2. <span data-ttu-id="a9cf4-115">**Atualização** utilizar das ferramentas da linha de comandos para PowerShell equivalentes, sempre que possível, para o melhor de auditoria e a experiência de personalização de JEA.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-115">**Update** use of command-line tools to PowerShell equivalents, where possible, for the best auditing and JEA customization experience.</span></span> <span data-ttu-id="a9cf4-116">Não podem ser restrita programas externos como granularmente como cmdlets do PowerShell nativo e funções no JEA.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-116">External programs can't be constrained as granularly as native PowerShell cmdlets and functions in JEA.</span></span>
3. <span data-ttu-id="a9cf4-117">**Restringir** o âmbito dos cmdlets para permitir apenas os parâmetros específicos ou valores de parâmetros.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-117">**Restrict** the scope of the cmdlets to only allow specific parameters or parameter values.</span></span> <span data-ttu-id="a9cf4-118">Isso é especialmente importante se os utilizadores devem gerir apenas parte de um sistema.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-118">This is especially important if users should manage only part of a system.</span></span>
4. <span data-ttu-id="a9cf4-119">**Criar** funções personalizadas para substituir comandos complexos ou comandos que são difíceis de restringir na JEA.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-119">**Create** custom functions to replace complex commands or commands that are difficult to constrain in JEA.</span></span> <span data-ttu-id="a9cf4-120">Uma função simples que encapsula um comando complexo ou se aplicar lógica de validação pode oferecer controle adicional para simplificar os administradores e utilizadores finais.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-120">A simple function that wraps a complex command or applies additional validation logic can offer additional control for admins and end-user simplicity.</span></span>
5. <span data-ttu-id="a9cf4-121">**Teste** a lista de âmbito de comandos permitidos com os seus utilizadores ou serviços de automatização e ajustar conforme necessário.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-121">**Test** the scoped list of allowable commands with your users or automation services, and adjust as necessary.</span></span>

### <a name="examples-of-potentially-dangerous-commands"></a><span data-ttu-id="a9cf4-122">Exemplos de comandos potencialmente perigosos</span><span class="sxs-lookup"><span data-stu-id="a9cf4-122">Examples of potentially dangerous commands</span></span>

<span data-ttu-id="a9cf4-123">Seleção de cuidado de comandos é importante certificar-se de que o ponto final da JEA não permite que o usuário elevar as respetivas permissões.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-123">Careful selection of commands is important to ensure the JEA endpoint doesn't allow the user to elevate their permissions.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="a9cf4-124">Informações essenciais necessárias para successCommands de usuário numa sessão JEA, muitas vezes, são executados com privilégios elevados.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-124">Essential information required for user successCommands in a JEA session are often run with elevated privileges.</span></span>

<span data-ttu-id="a9cf4-125">A tabela seguinte contém exemplos de comandos que podem ser usados maliciosamente se permitido num Estado irrestrita.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-125">The following table contains examples of commands that can be used maliciously if allowed in an unconstrained state.</span></span> <span data-ttu-id="a9cf4-126">Isso não é uma lista exaustiva e só deve ser utilizado como um ponto de partida cautelar de começar por.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-126">This isn't an exhaustive list and should only be used as a cautionary starting point.</span></span>

|                                            <span data-ttu-id="a9cf4-127">Risco</span><span class="sxs-lookup"><span data-stu-id="a9cf4-127">Risk</span></span>                                            |                                <span data-ttu-id="a9cf4-128">Exemplo</span><span class="sxs-lookup"><span data-stu-id="a9cf4-128">Example</span></span>                                |                                                                              <span data-ttu-id="a9cf4-129">Comandos relacionados</span><span class="sxs-lookup"><span data-stu-id="a9cf4-129">Related commands</span></span>                                                                              |
| ------------------------------------------------------------------------------------------ | --------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <span data-ttu-id="a9cf4-130">O usuário conectado a conceder privilégios de administrador para ignorar a JEA</span><span class="sxs-lookup"><span data-stu-id="a9cf4-130">Granting the connecting user admin privileges to bypass JEA</span></span>                                | `Add-LocalGroupMember -Member 'CONTOSO\jdoe' -Group 'Administrators'` | <span data-ttu-id="a9cf4-131">`Add-ADGroupMember`, `Add-LocalGroupMember`, `net.exe`, `dsadd.exe`</span><span class="sxs-lookup"><span data-stu-id="a9cf4-131">`Add-ADGroupMember`, `Add-LocalGroupMember`, `net.exe`, `dsadd.exe`</span></span>                                                                                                        |
| <span data-ttu-id="a9cf4-132">Executar código arbitrário, como software maligno, explorações ou scripts personalizados para ignorar as proteções</span><span class="sxs-lookup"><span data-stu-id="a9cf4-132">Running arbitrary code, such as malware, exploits, or custom scripts to bypass protections</span></span> | `Start-Process -FilePath '\\san\share\malware.exe'`                   | <span data-ttu-id="a9cf4-133">`Start-Process`, `New-Service`, `Invoke-Item`, `Invoke-WmiMethod`, `Invoke-CimMethod`, `Invoke-Expression`, `Invoke-Command`, `New-ScheduledTask`, `Register-ScheduledJob`</span><span class="sxs-lookup"><span data-stu-id="a9cf4-133">`Start-Process`, `New-Service`, `Invoke-Item`, `Invoke-WmiMethod`, `Invoke-CimMethod`, `Invoke-Expression`, `Invoke-Command`, `New-ScheduledTask`, `Register-ScheduledJob`</span></span> |

## <a name="create-a-role-capability-file"></a><span data-ttu-id="a9cf4-134">Crie um ficheiro de recurso de função</span><span class="sxs-lookup"><span data-stu-id="a9cf4-134">Create a role capability file</span></span>

<span data-ttu-id="a9cf4-135">Pode criar um novo arquivo de recurso de função de PowerShell com o [New-PSRoleCapabilityFile](/powershell/module/microsoft.powershell.core/new-psrolecapabilityfile?view=powershell-6) cmdlet.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-135">You can create a new PowerShell role capability file with the [New-PSRoleCapabilityFile](/powershell/module/microsoft.powershell.core/new-psrolecapabilityfile?view=powershell-6) cmdlet.</span></span>

```powershell
New-PSRoleCapabilityFile -Path .\MyFirstJEARole.psrc
```

<span data-ttu-id="a9cf4-136">O arquivo de recurso de função resultante deve ser editado para permitir que os comandos necessários para a função.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-136">The resulting role capability file should be edited to allow the commands required for the role.</span></span> <span data-ttu-id="a9cf4-137">A documentação de ajuda do PowerShell contém vários exemplos de como pode configurar o ficheiro.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-137">The PowerShell help documentation contains several examples of how you can configure the file.</span></span>

### <a name="allowing-powershell-cmdlets-and-functions"></a><span data-ttu-id="a9cf4-138">Permitir que as funções e cmdlets do PowerShell</span><span class="sxs-lookup"><span data-stu-id="a9cf4-138">Allowing PowerShell cmdlets and functions</span></span>

<span data-ttu-id="a9cf4-139">Para autorizar os utilizadores executem funções ou os cmdlets do PowerShell, adicione o nome do cmdlet ou uma função aos campos VisibleCmdlets ou VisibleFunctions.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-139">To authorize users to run PowerShell cmdlets or functions, add the cmdlet or function name to the VisibleCmdlets or VisibleFunctions fields.</span></span> <span data-ttu-id="a9cf4-140">Se não souber ao certo se um comando é um cmdlet ou uma função, pode executar `Get-Command <name>` e verifique a **CommandType** propriedade na saída.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-140">If you aren't sure whether a command is a cmdlet or function, you can run `Get-Command <name>` and check the **CommandType** property in the output.</span></span>

```powershell
VisibleCmdlets = 'Restart-Computer', 'Get-NetIPAddress'
```

<span data-ttu-id="a9cf4-141">Por vezes, o escopo de um cmdlet específico ou a função é demasiado abrangente para as necessidades dos seus utilizadores.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-141">Sometimes the scope of a specific cmdlet or function is too broad for your users' needs.</span></span> <span data-ttu-id="a9cf4-142">Um administrador de DNS, por exemplo, provavelmente só precisa de acesso para reiniciar o serviço DNS.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-142">A DNS admin, for example, probably only needs access to restart the DNS service.</span></span> <span data-ttu-id="a9cf4-143">Em ambientes de multi-inquilino, os inquilinos têm acesso a ferramentas de gestão self-service.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-143">In multi-tenant environments, tenants have access to self-service management tools.</span></span> <span data-ttu-id="a9cf4-144">Os inquilinos devem ser limitados a gerir os seus próprios recursos.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-144">Tenants should be limited to managing their own resources.</span></span> <span data-ttu-id="a9cf4-145">Nesses casos, pode restringir quais parâmetros são expostos a partir de uma função ou o cmdlet.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-145">For these cases, you can restrict which parameters are exposed from the cmdlet or function.</span></span>

```powershell
VisibleCmdlets = @{ Name = 'Restart-Computer'; Parameters = @{ Name = 'Name' }}
```

<span data-ttu-id="a9cf4-146">Em cenários mais avançados, também poderá restringir os valores de que um utilizador pode utilizar com estes parâmetros.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-146">In more advanced scenarios, you may also need to restrict the values a user may use with these parameters.</span></span> <span data-ttu-id="a9cf4-147">Funcionalidades de função permitem-lhe definir um conjunto de valores ou um padrão de expressão regular que determinam quais entrada é permitida.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-147">Role capabilities let you define a set of values or a regular expression pattern that determine what input is allowed.</span></span>

```powershell
VisibleCmdlets = @{ Name = 'Restart-Service'; Parameters = @{ Name = 'Name'; ValidateSet = 'Dns', 'Spooler' }},
                 @{ Name = 'Start-Website'; Parameters = @{ Name = 'Name'; ValidatePattern = 'HR_*' }}
```

> [!NOTE]
> <span data-ttu-id="a9cf4-148">O [parâmetros comuns do PowerShell](/powershell/module/microsoft.powershell.core/about/about_commonparameters) sempre são permitidos, mesmo que restrinja os parâmetros disponíveis.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-148">The [common PowerShell parameters](/powershell/module/microsoft.powershell.core/about/about_commonparameters) are always allowed, even if you restrict the available parameters.</span></span>
> <span data-ttu-id="a9cf4-149">Deve não explicitamente listá-los no campo de parâmetros.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-149">You should not explicitly list them in the Parameters field.</span></span>

<span data-ttu-id="a9cf4-150">A tabela abaixo descreve as várias formas como pode personalizar uma função ou o cmdlet visível.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-150">The table below describes the various ways you can customize a visible cmdlet or function.</span></span>
<span data-ttu-id="a9cf4-151">Pode combinar e corresponde a nenhum do abaixo na **VisibleCmdlets** campo.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-151">You can mix and match any of the below in the **VisibleCmdlets** field.</span></span>

|                                           <span data-ttu-id="a9cf4-152">Exemplo</span><span class="sxs-lookup"><span data-stu-id="a9cf4-152">Example</span></span>                                           |                                                             <span data-ttu-id="a9cf4-153">Caso de utilização</span><span class="sxs-lookup"><span data-stu-id="a9cf4-153">Use case</span></span>                                                              |
| ------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| <span data-ttu-id="a9cf4-154">`'My-Func'` ou `@{ Name = 'My-Func' }`</span><span class="sxs-lookup"><span data-stu-id="a9cf4-154">`'My-Func'` or `@{ Name = 'My-Func' }`</span></span>                                                      | <span data-ttu-id="a9cf4-155">Permite que o usuário execute `My-Func` sem quaisquer restrições nos parâmetros.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-155">Allows the user to run `My-Func` without any restrictions on the parameters.</span></span>                                                      |
| `'MyModule\My-Func'`                                                                        | <span data-ttu-id="a9cf4-156">Permite que o usuário execute `My-Func` do módulo `MyModule` sem quaisquer restrições nos parâmetros.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-156">Allows the user to run `My-Func` from the module `MyModule` without any restrictions on the parameters.</span></span>                           |
| `'My-*'`                                                                                    | <span data-ttu-id="a9cf4-157">Permite que o usuário execute qualquer cmdlet ou uma função com o verbo `My`.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-157">Allows the user to run any cmdlet or function with the verb `My`.</span></span>                                                                 |
| `'*-Func'`                                                                                  | <span data-ttu-id="a9cf4-158">Permite que o usuário execute qualquer cmdlet ou uma função com o substantivo `Func`.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-158">Allows the user to run any cmdlet or function with the noun `Func`.</span></span>                                                               |
| `@{ Name = 'My-Func'; Parameters = @{ Name = 'Param1'}, @{ Name = 'Param2' }}`              | <span data-ttu-id="a9cf4-159">Permite que o usuário execute `My-Func` com o `Param1` e `Param2` parâmetros.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-159">Allows the user to run `My-Func` with the `Param1` and `Param2` parameters.</span></span> <span data-ttu-id="a9cf4-160">Qualquer valor pode ser fornecido para os parâmetros.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-160">Any value can be supplied to the parameters.</span></span>          |
| `@{ Name = 'My-Func'; Parameters = @{ Name = 'Param1'; ValidateSet = 'Value1', 'Value2' }}` | <span data-ttu-id="a9cf4-161">Permite que o usuário execute `My-Func` com o `Param1` parâmetro.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-161">Allows the user to run `My-Func` with the `Param1` parameter.</span></span> <span data-ttu-id="a9cf4-162">Apenas "Value1" e "Value2" podem ser fornecidos para o parâmetro.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-162">Only "Value1" and "Value2" can be supplied to the parameter.</span></span>        |
| `@{ Name = 'My-Func'; Parameters = @{ Name = 'Param1'; ValidatePattern = 'contoso.*' }}`    | <span data-ttu-id="a9cf4-163">Permite que o usuário execute `My-Func` com o `Param1` parâmetro.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-163">Allows the user to run `My-Func` with the `Param1` parameter.</span></span> <span data-ttu-id="a9cf4-164">Qualquer valor a partir do "contoso" pode ser fornecido para o parâmetro.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-164">Any value starting with "contoso" can be supplied to the parameter.</span></span> |

> [!WARNING]
> <span data-ttu-id="a9cf4-165">Para melhores práticas de segurança, não é recomendado para utilizar carateres universais, quando se definem cmdlets visível ou funções.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-165">For best security practices, it is not recommended to use wildcards when defining visible cmdlets or functions.</span></span> <span data-ttu-id="a9cf4-166">Em vez disso, deve listar explicitamente cada comando confiável para garantir que nenhum outro comando que partilham o mesmo esquema de nomenclatura inadvertidamente está autorizado.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-166">Instead, you should explicitly list each trusted command to ensure no other commands that share the same naming scheme are unintentionally authorized.</span></span>

<span data-ttu-id="a9cf4-167">Não é possível aplicar ambos um **ValidatePattern** e **ValidateSet** para o mesmo cmdlet ou função.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-167">You can't apply both a **ValidatePattern** and **ValidateSet** to the same cmdlet or function.</span></span>

<span data-ttu-id="a9cf4-168">Se o fizer, o **ValidatePattern** substitui o **ValidateSet**.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-168">If you do, the **ValidatePattern** overrides the **ValidateSet**.</span></span>

<span data-ttu-id="a9cf4-169">Para obter mais informações sobre **ValidatePattern**, confira [isso *Hey, Scripting Guy!* publicar](https://devblogs.microsoft.com/scripting/validate-powershell-parameters-before-running-the-script/) e o [expressões regulares do PowerShell](/powershell/module/microsoft.powershell.core/about/about_regular_expressions)conteúdo de referência.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-169">For more information about **ValidatePattern**, check out [this *Hey, Scripting Guy!* post](https://devblogs.microsoft.com/scripting/validate-powershell-parameters-before-running-the-script/) and the [PowerShell Regular Expressions](/powershell/module/microsoft.powershell.core/about/about_regular_expressions) reference content.</span></span>

### <a name="allowing-external-commands-and-powershell-scripts"></a><span data-ttu-id="a9cf4-170">Permitir que os comandos externos e scripts do PowerShell</span><span class="sxs-lookup"><span data-stu-id="a9cf4-170">Allowing external commands and PowerShell scripts</span></span>

<span data-ttu-id="a9cf4-171">Para permitir que os usuários executem executáveis e scripts do PowerShell (. ps1) numa sessão JEA, é necessário adicionar o caminho completo para cada programa no **VisibleExternalCommands** campo.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-171">To allow users to run executables and PowerShell scripts (.ps1) in a JEA session, you have to add the full path to each program in the **VisibleExternalCommands** field.</span></span>

```powershell
VisibleExternalCommands = 'C:\Windows\System32\whoami.exe', 'C:\Program Files\Contoso\Scripts\UpdateITSoftware.ps1'
```

<span data-ttu-id="a9cf4-172">Sempre que possível, para utilizar o PowerShell equivalentes cmdlet ou uma função para quaisquer executáveis externos que autorizar uma vez que têm controlo sobre os parâmetros permitidos com as funções e cmdlets do PowerShell.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-172">Where possible, to use PowerShell cmdlet or function equivalents for any external executables you authorize since you have control over the parameters allowed with PowerShell cmdlets and functions.</span></span>

<span data-ttu-id="a9cf4-173">Número de executáveis permite-lhe ler o estado atual e, em seguida, altere-o ao fornecer parâmetros diferentes.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-173">Many executables allow you to read the current state and then change it by providing different parameters.</span></span>

<span data-ttu-id="a9cf4-174">Por exemplo, considere a função de um administrador de servidor de ficheiro que gerencia compartilhamentos de rede hospedados num sistema.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-174">For example, consider the role of a file server admin that manages network shares hosted on a system.</span></span> <span data-ttu-id="a9cf4-175">Uma forma de gerir partilhas é usar `net share`.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-175">One way of managing shares is to use `net share`.</span></span> <span data-ttu-id="a9cf4-176">No entanto, permitindo **net.exe** é perigoso porque o utilizador poderá utilizar o comando para obter privilégios de administrador com `net group Administrators unprivilegedjeauser /add`.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-176">However, allowing **net.exe** is dangerous because the user could use the command to gain admin privileges with `net group Administrators unprivilegedjeauser /add`.</span></span> <span data-ttu-id="a9cf4-177">Uma opção mais segura é permitir que [Get-SmbShare](/powershell/module/smbshare/get-smbshare), que ofereça o mesmo resultado, mas tem um muito mais âmbito limitado.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-177">A more secure option is to allow [Get-SmbShare](/powershell/module/smbshare/get-smbshare), which achieves the same result but has a much more limited scope.</span></span>

<span data-ttu-id="a9cf4-178">Ao disponibilizar comandos externos para os usuários numa sessão JEA, sempre Especifica o caminho completo para o executável.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-178">When making external commands available to users in a JEA session, always specify the complete path to the executable.</span></span> <span data-ttu-id="a9cf4-179">Isto impede que a execução da mesma forma nomeados e potencialmente maliciosos de programas de localizado em outro lugar no sistema.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-179">This prevents the execution of similarly named and potentially malicious programs located elsewhere on the system.</span></span>

### <a name="allowing-access-to-powershell-providers"></a><span data-ttu-id="a9cf4-180">Permitir o acesso aos provedores de PowerShell</span><span class="sxs-lookup"><span data-stu-id="a9cf4-180">Allowing access to PowerShell providers</span></span>

<span data-ttu-id="a9cf4-181">Por predefinição, não existem fornecedores de PowerShell estão disponíveis em sessões JEA.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-181">By default, no PowerShell providers are available in JEA sessions.</span></span> <span data-ttu-id="a9cf4-182">Isso reduz o risco de informações confidenciais e definições de configuração que está a ser reveladas para o usuário conectado.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-182">This reduces the risk of sensitive information and configuration settings being disclosed to the connecting user.</span></span>

<span data-ttu-id="a9cf4-183">Quando for necessário, pode permitir o acesso para os fornecedores de PowerShell utilizando o `VisibleProviders` comando.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-183">When necessary, you can allow access to the PowerShell providers using the `VisibleProviders` command.</span></span> <span data-ttu-id="a9cf4-184">Para obter uma lista completa dos fornecedores, executar `Get-PSProvider`.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-184">For a full list of providers, run `Get-PSProvider`.</span></span>

```powershell
VisibleProviders = 'Registry'
```

<span data-ttu-id="a9cf4-185">Para tarefas simples que necessitam de acesso ao sistema de arquivos, registro, arquivo de certificados ou outros fornecedores de serviços confidenciais, considere que escrever uma função personalizada que funciona com o fornecedor em nome do utilizador.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-185">For simple tasks that require access to the file system, registry, certificate store, or other sensitive providers, consider writing a custom function that works with the provider on the user's behalf.</span></span> <span data-ttu-id="a9cf4-186">As funções, cmdlets e programas externos disponíveis numa sessão JEA não são sujeitos às mesmas restrições quanto como JEA.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-186">The functions, cmdlets, and external programs available in a JEA session aren't subject to the same constraints as JEA.</span></span> <span data-ttu-id="a9cf4-187">Podem aceder a qualquer fornecedor por padrão.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-187">They can access any provider by default.</span></span> <span data-ttu-id="a9cf4-188">Considere também a utilização a [unidade utilizador](session-configurations.md#user-drive) quando é necessário copiar os ficheiros de ou para um ponto final JEA.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-188">Also consider using the [user drive](session-configurations.md#user-drive) when copying files to or from a JEA endpoint is required.</span></span>

### <a name="creating-custom-functions"></a><span data-ttu-id="a9cf4-189">Criar funções personalizadas</span><span class="sxs-lookup"><span data-stu-id="a9cf4-189">Creating custom functions</span></span>

<span data-ttu-id="a9cf4-190">Pode criar funções personalizadas num arquivo de recurso de função para simplificar as tarefas complexas para seus usuários finais.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-190">You can author custom functions in a role capability file to simplify complex tasks for your end users.</span></span> <span data-ttu-id="a9cf4-191">Funções personalizadas também são úteis quando necessitar de lógica de validação avançada para valores de parâmetros de cmdlet.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-191">Custom functions are also useful when you require advanced validation logic for cmdlet parameter values.</span></span> <span data-ttu-id="a9cf4-192">Pode escrever funções simples no **FunctionDefinitions** campo:</span><span class="sxs-lookup"><span data-stu-id="a9cf4-192">You can write simple functions in the **FunctionDefinitions** field:</span></span>

```powershell
VisibleFunctions = 'Get-TopProcess'

FunctionDefinitions = @{
    Name = 'Get-TopProcess'

    ScriptBlock = {
        param($Count = 10)

        Get-Process | Sort-Object -Property CPU -Descending |
            Microsoft.PowerShell.Utility\Select-Object -First $Count
    }
}
```

> [!IMPORTANT]
> <span data-ttu-id="a9cf4-193">Não se esqueça de adicionar o nome das suas funções personalizadas para o **VisibleFunctions** campo para que podem ser executadas pelos utilizadores JEA.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-193">Don't forget to add the name of your custom functions to the **VisibleFunctions** field so they can be run by the JEA users.</span></span>

<span data-ttu-id="a9cf4-194">O corpo (bloco de script) de funções personalizadas é executado no modo de idioma predefinido para o sistema e não está sujeita a restrições de idioma da JEA.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-194">The body (script block) of custom functions runs in the default language mode for the system and isn't subject to JEA's language constraints.</span></span> <span data-ttu-id="a9cf4-195">Isso significa que as funções podem aceder ao sistema de arquivos e registro e executar comandos que não ficam visíveis no arquivo de recurso de função.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-195">This means that functions can access the file system and registry, and run commands that weren't made visible in the role capability file.</span></span> <span data-ttu-id="a9cf4-196">Tenha cuidado para evitar executar código arbitrário quando o uso de parâmetros.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-196">Take care to avoid running arbitrary code when using parameters.</span></span> <span data-ttu-id="a9cf4-197">Evitar diretamente para os cmdlets, como a entrada do usuário de piping `Invoke-Expression`.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-197">Avoid piping user input directly into cmdlets like `Invoke-Expression`.</span></span>

<span data-ttu-id="a9cf4-198">No exemplo acima, tenha em atenção que o nome de módulo completamente qualificado (FQMN) `Microsoft.PowerShell.Utility\Select-Object` foi utilizado em vez da forma abreviada `Select-Object`.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-198">In the above example, notice that the fully qualified module name (FQMN) `Microsoft.PowerShell.Utility\Select-Object` was used instead of the shorthand `Select-Object`.</span></span>
<span data-ttu-id="a9cf4-199">Funções definidas nos arquivos de recurso de função são ainda pode ser o escopo de sessões JEA, que inclui as funções de proxy JEA cria para restringir comandos existentes.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-199">Functions defined in role capability files are still subject to the scope of JEA sessions, which includes the proxy functions JEA creates to constrain existing commands.</span></span>

<span data-ttu-id="a9cf4-200">Por predefinição, `Select-Object` é um cmdlet restrito em todas as sessões JEA que não permite a seleção das propriedades arbitrárias em objetos.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-200">By default, `Select-Object` is a constrained cmdlet in all JEA sessions that doesn't allow the selection of arbitrary properties on objects.</span></span> <span data-ttu-id="a9cf4-201">Para utilizar o irrestrita `Select-Object` em funções, tem de solicitar explicitamente a implementação completa com o FQMN.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-201">To use the unconstrained `Select-Object` in functions, you must explicitly request the full implementation using the FQMN.</span></span> <span data-ttu-id="a9cf4-202">Qualquer cmdlet restrita numa sessão JEA tem as mesmas restrições quanto quando invocado a partir de uma função.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-202">Any constrained cmdlet in a JEA session has the same constraints when invoked from a function.</span></span> <span data-ttu-id="a9cf4-203">Para obter mais informações, consulte [about_Command_Precedence](/powershell/module/microsoft.powershell.core/about/about_command_precedence).</span><span class="sxs-lookup"><span data-stu-id="a9cf4-203">For more information, see [about_Command_Precedence](/powershell/module/microsoft.powershell.core/about/about_command_precedence).</span></span>

<span data-ttu-id="a9cf4-204">Se estiver escrevendo diversas funções personalizadas, é mais conveniente de colocá-los num módulo de script do PowerShell.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-204">If you're writing several custom functions, it's more convenient to put them in a PowerShell script module.</span></span> <span data-ttu-id="a9cf4-205">Tornar essas funções visíveis no JEA sessão com o **VisibleFunctions** campo como faria com módulos internos e externos.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-205">You make those functions visible in the JEA session using the **VisibleFunctions** field like you would with built-in and third-party modules.</span></span>

<span data-ttu-id="a9cf4-206">Para o separador conclusão funcione corretamente em sessões JEA, tem de incluir a função incorporada `tabexpansion2` no **VisibleFunctions** lista.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-206">For tab completion to work properly in JEA sessions you must include the built-in function `tabexpansion2` in the **VisibleFunctions** list.</span></span>

## <a name="place-role-capabilities-in-a-module"></a><span data-ttu-id="a9cf4-207">Funcionalidades de função local num módulo</span><span class="sxs-lookup"><span data-stu-id="a9cf4-207">Place role capabilities in a module</span></span>

<span data-ttu-id="a9cf4-208">Por ordem para o PowerShell localizar um ficheiro de recurso de função, devem ser armazenado num **RoleCapabilities** pasta num módulo do PowerShell.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-208">In order for PowerShell to find a role capability file, it must be stored in a **RoleCapabilities** folder in a PowerShell module.</span></span> <span data-ttu-id="a9cf4-209">O módulo pode ser armazenado em qualquer pasta incluída no `$env:PSModulePath` variável de ambiente, no entanto não deve colocá-lo na System32 ou uma pasta em que o não fidedigno, os utilizadores a ligar foi possível modificar os ficheiros.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-209">The module can be stored in any folder included in the `$env:PSModulePath` environment variable, however you shouldn't place it in System32 or a folder where the untrusted, connecting users could modify the files.</span></span> <span data-ttu-id="a9cf4-210">Segue-se um exemplo de criação de um módulo de script de PowerShell básico chamado **ContosoJEA** no `$env:ProgramFiles` caminho.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-210">Below is an example of creating a basic PowerShell script module called **ContosoJEA** in the `$env:ProgramFiles` path.</span></span>

```powershell
# Create a folder for the module
$modulePath = Join-Path $env:ProgramFiles "WindowsPowerShell\Modules\ContosoJEA"
New-Item -ItemType Directory -Path $modulePath

# Create an empty script module and module manifest.
# At least one file in the module folder must have the same name as the folder itself.
New-Item -ItemType File -Path (Join-Path $modulePath "ContosoJEAFunctions.psm1")
New-ModuleManifest -Path (Join-Path $modulePath "ContosoJEA.psd1") -RootModule "ContosoJEAFunctions.psm1"

# Create the RoleCapabilities folder and copy in the PSRC file
$rcFolder = Join-Path $modulePath "RoleCapabilities"
New-Item -ItemType Directory $rcFolder
Copy-Item -Path .\MyFirstJEARole.psrc -Destination $rcFolder
```

<span data-ttu-id="a9cf4-211">Para obter mais informações sobre os módulos do PowerShell, consulte [Noções básicas sobre um módulo do PowerShell](/powershell/developer/windows-powershell).</span><span class="sxs-lookup"><span data-stu-id="a9cf4-211">For more information about PowerShell modules, see [Understanding a PowerShell Module](/powershell/developer/windows-powershell).</span></span>

## <a name="updating-role-capabilities"></a><span data-ttu-id="a9cf4-212">Atualizar funcionalidades de função</span><span class="sxs-lookup"><span data-stu-id="a9cf4-212">Updating role capabilities</span></span>

<span data-ttu-id="a9cf4-213">Pode editar um arquivo de recurso de função para atualizar as definições em qualquer altura.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-213">You can edit a role capability file to update the settings at any time.</span></span> <span data-ttu-id="a9cf4-214">Quaisquer novas sessões JEA iniciados depois do recurso de função foi atualizado irão refletir as capacidades revisadas.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-214">Any new JEA sessions started after the role capability has been updated will reflect the revised capabilities.</span></span>

<span data-ttu-id="a9cf4-215">É por isso controlar o acesso para a pasta de recursos de função é tão importante.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-215">This is why controlling access to the role capabilities folder is so important.</span></span> <span data-ttu-id="a9cf4-216">Apenas administradores altamente fidedignos devem ter permissão para alterar os arquivos de recurso de função.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-216">Only highly trusted administrators should be allowed to change role capability files.</span></span> <span data-ttu-id="a9cf4-217">Se um usuário não confiável pode alterar os arquivos de recurso de função, eles podem facilmente atribuir acesso para cmdlets que permitem-los elevar seus privilégios.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-217">If an untrusted user can change role capability files, they can easily give themselves access to cmdlets that allow them to elevate their privileges.</span></span>

<span data-ttu-id="a9cf4-218">Para administradores que procuram para bloquear o acesso a funcionalidades de função, certifique-se de que o sistema Local tem acesso de leitura aos ficheiros de recurso de função e módulos que contêm.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-218">For administrators looking to lock down access to the role capabilities, ensure Local System has read access to the role capability files and containing modules.</span></span>

## <a name="how-role-capabilities-are-merged"></a><span data-ttu-id="a9cf4-219">Como as funcionalidades de função são intercaladas</span><span class="sxs-lookup"><span data-stu-id="a9cf4-219">How role capabilities are merged</span></span>

<span data-ttu-id="a9cf4-220">Os utilizadores que terão acesso a todas as capacidades de função correspondente no [o ficheiro de configuração de sessão](session-configurations.md) quando entrarão numa sessão JEA.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-220">Users are granted access to all matching role capabilities in the [session configuration file](session-configurations.md) when they enter a JEA session.</span></span> <span data-ttu-id="a9cf4-221">JEA tenta dar ao usuário o conjunto mais permissivo de comandos permitidos por qualquer uma das funções.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-221">JEA tries to give the user the most permissive set of commands allowed by any of the roles.</span></span>

### <a name="visiblecmdlets-and-visiblefunctions"></a><span data-ttu-id="a9cf4-222">VisibleCmdlets e VisibleFunctions</span><span class="sxs-lookup"><span data-stu-id="a9cf4-222">VisibleCmdlets and VisibleFunctions</span></span>

<span data-ttu-id="a9cf4-223">A lógica mais complexa de intercalação afeta cmdlets e funções, o que podem ter seus parâmetros e valores de parâmetro nos JEA.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-223">The most complex merge logic affects cmdlets and functions, which can have their parameters and parameter values limited in JEA.</span></span>

<span data-ttu-id="a9cf4-224">As regras são os seguintes:</span><span class="sxs-lookup"><span data-stu-id="a9cf4-224">The rules are as follows:</span></span>

1. <span data-ttu-id="a9cf4-225">Se um cmdlet só ficam visível numa função, é visível ao utilizador com quaisquer restrições de parâmetro aplicável.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-225">If a cmdlet is only made visible in one role, it is visible to the user with any applicable parameter constraints.</span></span>
2. <span data-ttu-id="a9cf4-226">Se um cmdlet ficam visível em mais de uma função, e cada função tem as mesmas restrições sobre o cmdlet, o cmdlet é visível para o usuário com essas restrições.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-226">If a cmdlet is made visible in more than one role, and each role has the same constraints on the cmdlet, the cmdlet is visible to the user with those constraints.</span></span>
3. <span data-ttu-id="a9cf4-227">Se um cmdlet ficam visível em mais de uma função, e cada função permite que um conjunto diferente de parâmetros, o cmdlet e todos os parâmetros definidos em cada função estão visíveis para o utilizador.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-227">If a cmdlet is made visible in more than one role, and each role allows a different set of parameters, the cmdlet and all the parameters defined across every role are visible to the user.</span></span>
   <span data-ttu-id="a9cf4-228">Se uma função não tiver restrições nos parâmetros, todos os parâmetros são permitidos.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-228">If one role doesn't have constraints on the parameters, all parameters are allowed.</span></span>
4. <span data-ttu-id="a9cf4-229">Se uma função define um conjunto de validar ou validar padrão para um parâmetro de cmdlet, e a outra função permite que o parâmetro, mas não restrinja os valores de parâmetros, o conjunto de validar ou o padrão é ignorada.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-229">If one role defines a validate set or validate pattern for a cmdlet parameter, and the other role allows the parameter but does not constrain the parameter values, the validate set or pattern is ignored.</span></span>
5. <span data-ttu-id="a9cf4-230">Se um conjunto de validar for definido para o mesmo parâmetro de cmdlet em mais de uma função, todos os valores de todos os conjuntos de validar são permitidos.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-230">If a validate set is defined for the same cmdlet parameter in more than one role, all values from all validate sets are allowed.</span></span>
6. <span data-ttu-id="a9cf4-231">Se um padrão de validar for definido para o mesmo parâmetro de cmdlet em mais de uma função, são permitidos quaisquer valores que correspondam a qualquer um dos padrões.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-231">If a validate pattern is defined for the same cmdlet parameter in more than one role, any values that match any of the patterns are allowed.</span></span>
7. <span data-ttu-id="a9cf4-232">Se um conjunto de validar é definido numa ou mais funções e um padrão de validar é definido em outra função para o mesmo parâmetro de cmdlet, o conjunto de validar é ignorado e regra (6) aplica-se para os padrões de validar restantes.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-232">If a validate set is defined in one or more roles, and a validate pattern is defined in another role for the same cmdlet parameter, the validate set is ignored and rule (6) applies to the remaining validate patterns.</span></span>

<span data-ttu-id="a9cf4-233">Segue-se um exemplo de como as funções são mescladas, de acordo com essas regras:</span><span class="sxs-lookup"><span data-stu-id="a9cf4-233">Below is an example of how roles are merged according to these rules:</span></span>

```powershell
# Role A Visible Cmdlets
$roleA = @{
    VisibleCmdlets = 'Get-Service',
                     @{ Name = 'Restart-Service';
                        Parameters = @{ Name = 'DisplayName'; ValidateSet = 'DNS Client' } }
}

# Role B Visible Cmdlets
$roleB = @{
    VisibleCmdlets = @{ Name = 'Get-Service';
                        Parameters = @{ Name = 'DisplayName'; ValidatePattern = 'DNS.*' } },
                     @{ Name = 'Restart-Service';
                        Parameters = @{ Name = 'DisplayName'; ValidateSet = 'DNS Server' } }
}

# Resulting permissions for a user who belongs to both role A and B
# - The constraint in role B for the DisplayName parameter on Get-Service
#   is ignored because of rule #4
# - The ValidateSets for Restart-Service are merged because both roles use
#   ValidateSet on the same parameter per rule #5
$mergedAandB = @{
    VisibleCmdlets = 'Get-Service',
                     @{ Name = 'Restart-Service';
                        Parameters = @{ Name = 'DisplayName'; ValidateSet = 'DNS Client', 'DNS Server' } }
}
```

### <a name="visibleexternalcommands-visiblealiases-visibleproviders-scriptstoprocess"></a><span data-ttu-id="a9cf4-234">VisibleExternalCommands, VisibleAliases, VisibleProviders, ScriptsToProcess</span><span class="sxs-lookup"><span data-stu-id="a9cf4-234">VisibleExternalCommands, VisibleAliases, VisibleProviders, ScriptsToProcess</span></span>

<span data-ttu-id="a9cf4-235">Todos os outros campos no ficheiro de recurso de função são adicionados a um conjunto cumulativo de comandos externos permitidos, aliases, provedores e scripts de inicialização.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-235">All other fields in the role capability file are added to a cumulative set of allowable external commands, aliases, providers, and startup scripts.</span></span> <span data-ttu-id="a9cf4-236">Qualquer comando, o alias, o fornecedor ou o script disponível num recurso de função está disponível para o utilizador JEA.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-236">Any command, alias, provider, or script available in one role capability is available to the JEA user.</span></span>

<span data-ttu-id="a9cf4-237">Tenha cuidado para garantir que o conjunto combinado de provedores a partir de um recurso de função e cmdlets/funções/comandos a partir de outro não permitir que os utilizadores não intencional acesso aos recursos do sistema.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-237">Be careful to ensure that the combined set of providers from one role capability and cmdlets/functions/commands from another don't allow users unintentional access to system resources.</span></span>
<span data-ttu-id="a9cf4-238">Por exemplo, se uma função permite que o `Remove-Item` cmdlet e outro permite que o `FileSystem` fornecedor, está em risco de um utilizador JEA a eliminar ficheiros arbitrários no seu computador.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-238">For example, if one role allows the `Remove-Item` cmdlet and another allows the `FileSystem` provider, you are at risk of a JEA user deleting arbitrary files on your computer.</span></span> <span data-ttu-id="a9cf4-239">Pode encontrar informações adicionais sobre como identificar as permissões efetivas dos utilizadores no [auditoria JEA](audit-and-report.md) artigo.</span><span class="sxs-lookup"><span data-stu-id="a9cf4-239">Additional information about identifying users' effective permissions can be found in the [auditing JEA](audit-and-report.md) article.</span></span>

## <a name="next-steps"></a><span data-ttu-id="a9cf4-240">Passos Seguintes</span><span class="sxs-lookup"><span data-stu-id="a9cf4-240">Next steps</span></span>

[<span data-ttu-id="a9cf4-241">Criar um ficheiro de configuração de sessão</span><span class="sxs-lookup"><span data-stu-id="a9cf4-241">Create a session configuration file</span></span>](session-configurations.md)
