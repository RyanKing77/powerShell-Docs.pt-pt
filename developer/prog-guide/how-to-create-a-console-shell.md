---
title: Como criar um Shell do Console | Documentos da Microsoft
ms.custom: ''
ms.date: 09/13/2016
ms.reviewer: ''
ms.suite: ''
ms.tgt_pltfrm: ''
ms.topic: article
helpviewer_keywords:
- Make-Shell [PowerShell Programmer's Guide]
ms.assetid: 6c24dd44-a8ec-421d-ac86-90912e1a8cc6
caps.latest.revision: 5
ms.openlocfilehash: 7166881bd1403ea8c81ec2928321f6b93e3ac58d
ms.sourcegitcommit: b6871f21bd666f9cd71dd336bb3f844cf472b56c
ms.translationtype: MT
ms.contentlocale: pt-PT
ms.lasthandoff: 02/03/2019
ms.locfileid: "56845060"
---
# <a name="how-to-create-a-console-shell"></a><span data-ttu-id="bf3e2-102">How to Create a Console Shell (Como Criar uma Shell da Consola)</span><span class="sxs-lookup"><span data-stu-id="bf3e2-102">How to Create a Console Shell</span></span>

<span data-ttu-id="bf3e2-103">Windows PowerShell fornece uma ferramenta de Make-Shell, também referida como o "marca-kit", que é utilizado para criar um shell de consola que não é extensível.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-103">Windows PowerShell provides a Make-Shell tool, also referred to as the "make-kit", that is used to create a console shell that is not extensible.</span></span> <span data-ttu-id="bf3e2-104">Shells criados com essa nova ferramenta não podem ser expandidas posteriormente por meio de um snap-in do Windows PowerShell.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-104">Shells created with this new tool cannot be extended later through a Windows PowerShell snap-in.</span></span>

## <a name="syntax"></a><span data-ttu-id="bf3e2-105">Sintaxe</span><span class="sxs-lookup"><span data-stu-id="bf3e2-105">Syntax</span></span>

<span data-ttu-id="bf3e2-106">Segue-se a sintaxe usada para executar Make-Shell a partir de dentro de um arquivo de marca.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-106">Here is the syntax used to run Make-Shell from within a make-file.</span></span>

```
make-shell
  -out n.exe
  -namespace ns
  [ -lib libdirectory1[,libdirectory2,..] ]
  [ -reference ca1.dll[,ca2.dll,...] ]
  [ -formatdata fd1.format.ps1xml[,fd2.format.ps1xml,...] ]
  [ -typedata td1.type.ps1xml[,td2.type.ps1xml,...] ]
  [ -source c1.cs [,c2.cs,...] ]
  [ -authorizationmanager authorizationManagerType ]
  [ -win32icon i.ico ]
  [ -initscript p.ps1 ]
  [ -builtinscript s1.ps1[,s2.ps1,...] ]
  [ -resource resourcefile.txt ]
  [ -cscflags cscFlags ]
  [ -? | -help ]
```

## <a name="parameters"></a><span data-ttu-id="bf3e2-107">Parâmetros</span><span class="sxs-lookup"><span data-stu-id="bf3e2-107">Parameters</span></span>

<span data-ttu-id="bf3e2-108">Eis uma breve descrição dos parâmetros de Make-Shell.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-108">Here is a brief description of the parameters of Make-Shell.</span></span>

> [!CAUTION]
> <span data-ttu-id="bf3e2-109">Caminhos UNC para assemblies não são suportados pelo Make-Shell.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-109">UNC paths to assemblies are not supported by Make-Shell.</span></span>

|<span data-ttu-id="bf3e2-110">Parâmetro</span><span class="sxs-lookup"><span data-stu-id="bf3e2-110">Parameter</span></span>|<span data-ttu-id="bf3e2-111">Descrição</span><span class="sxs-lookup"><span data-stu-id="bf3e2-111">Description</span></span>|
|---------------|-----------------|
|<span data-ttu-id="bf3e2-112">-out n.exe</span><span class="sxs-lookup"><span data-stu-id="bf3e2-112">-out n.exe</span></span>|<span data-ttu-id="bf3e2-113">Obrigatório.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-113">Required.</span></span> <span data-ttu-id="bf3e2-114">O nome do shell para produzir.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-114">The name of the shell to produce.</span></span> <span data-ttu-id="bf3e2-115">O caminho é especificado como parte deste parâmetro.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-115">The path is specified as part of this parameter.</span></span><br /><br /> <span data-ttu-id="bf3e2-116">Make-shell serão acrescentadas ".exe" a este valor se não for especificado.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-116">Make-shell will append ".exe" to this value if it is not specified.</span></span> <span data-ttu-id="bf3e2-117">**Atenção:**  Não crie um ficheiro de saída com o mesmo nome que o ficheiro. dll referenciada.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-117">**Caution:**  Do not create an output file with the same name as the referenced .dll file.</span></span> <span data-ttu-id="bf3e2-118">Se tentar isso, a ferramenta de Make-Shell cria um arquivo. cs com o mesmo nome, o que irá substituir o ficheiro. CS que tem o seu código-fonte cmdlet.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-118">If you attempt this, the Make-Shell tool creates a .cs file with the same name, which will overwrite the .cs file that has your cmdlet source code.</span></span>|
|<span data-ttu-id="bf3e2-119">-namespace ns</span><span class="sxs-lookup"><span data-stu-id="bf3e2-119">-namespace ns</span></span>|<span data-ttu-id="bf3e2-120">Obrigatório.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-120">Required.</span></span> <span data-ttu-id="bf3e2-121">O espaço de nomes a utilizar para a derivada [System.Management.Automation.Runspaces.Runspaceconfiguration](/dotnet/api/System.Management.Automation.Runspaces.RunspaceConfiguration) classe que o kit de marca gera e compila.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-121">The namespace to use for the derived [System.Management.Automation.Runspaces.Runspaceconfiguration](/dotnet/api/System.Management.Automation.Runspaces.RunspaceConfiguration) class that the make-kit generates and compiles.</span></span>|
|<span data-ttu-id="bf3e2-122">-lib libdirectory1 [, libdirectory2,...]</span><span class="sxs-lookup"><span data-stu-id="bf3e2-122">-lib libdirectory1[,libdirectory2,..]</span></span>|<span data-ttu-id="bf3e2-123">Os diretórios que serão pesquisados assemblies do .NET, incluindo os assemblies do Windows PowerShell, assemblies especificados pelo `reference` parâmetro, assemblagens referenciadas indiretamente pelo outro assembly e os assemblies de system .NET.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-123">The directories that are searched for .NET assemblies, including the Windows PowerShell assemblies, assemblies specified by the `reference` parameter, assemblies indirectly referenced by another assembly, and the .NET system assemblies.</span></span>|
|<span data-ttu-id="bf3e2-124">-reference ca1.dll[,ca2.dll,...]</span><span class="sxs-lookup"><span data-stu-id="bf3e2-124">-reference ca1.dll[,ca2.dll,...]</span></span>|<span data-ttu-id="bf3e2-125">Uma lista separada por vírgulas dos assemblies para incluir no shell.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-125">A comma-separated list of the assemblies to include in the shell.</span></span> <span data-ttu-id="bf3e2-126">Esses assemblies inclui todos os cmdlet e os assemblies de fornecedor, bem como assemblies de recursos que devem ser carregados.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-126">These assemblies  includes all cmdlet and provider assemblies, as well as resource assemblies that should be loaded.</span></span> <span data-ttu-id="bf3e2-127">Se este parâmetro não for especificado, em seguida, um shell é produzido que contém apenas os principais cmdlets e fornecedores fornecidos pelo Windows PowerShell.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-127">If this parameter is not specified, then a shell is produced that contains only the core cmdlets and providers provided by Windows PowerShell.</span></span><br /><br /> <span data-ttu-id="bf3e2-128">Os assemblies podem ser especificados usando o caminho completo, caso contrário, serão procurados para utilizar o caminho especificado pelo `lib` parâmetro.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-128">The assemblies can be specified using their full path, otherwise they will be searched for using the path specified by the `lib` parameter.</span></span>|
|<span data-ttu-id="bf3e2-129">-formatdata fd1.format.ps1xml[,fd2.format.ps1xml,...]</span><span class="sxs-lookup"><span data-stu-id="bf3e2-129">-formatdata fd1.format.ps1xml[,fd2.format.ps1xml,...]</span></span>|<span data-ttu-id="bf3e2-130">Uma lista separada por vírgulas de dados de formato para incluir no shell.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-130">A comma-separated list of format data to include in the shell.</span></span> <span data-ttu-id="bf3e2-131">Se este parâmetro não for especificado, em seguida, um shell é produzido que contém os dados do formato fornecidos pelo Windows PowerShell.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-131">If this parameter is not specified, then a shell is produced that contains only the format data provided by Windows PowerShell.</span></span>|
|<span data-ttu-id="bf3e2-132">-typedata td1.type.ps1xml[,td2.type.ps1xml,...]</span><span class="sxs-lookup"><span data-stu-id="bf3e2-132">-typedata td1.type.ps1xml[,td2.type.ps1xml,...]</span></span>|<span data-ttu-id="bf3e2-133">Uma lista separada por vírgulas de dados de tipo para incluir no shell.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-133">A comma-separated list of type data to include in the shell.</span></span> <span data-ttu-id="bf3e2-134">Se este parâmetro não for especificado, em seguida, um shell é produzido que contém apenas os dados de tipo fornecidos pelo Windows PowerShell.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-134">If this parameter is not specified, then a shell is produced that contains only the type data provided by Windows PowerShell.</span></span>|
|<span data-ttu-id="bf3e2-135">-source c1.cs [,c2.cs,...]</span><span class="sxs-lookup"><span data-stu-id="bf3e2-135">-source c1.cs [,c2.cs,...]</span></span>|<span data-ttu-id="bf3e2-136">O nome de um ficheiro, fornecido pelo desenvolvedor do shell, que contém qualquer código de origem necessário para criar o shell.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-136">The name of a file, provided by the shell developer, that contains any source code needed to build the shell.</span></span><br /><br /> <span data-ttu-id="bf3e2-137">O arquivo de código de origem pode conter nenhum do seguinte código de origem:</span><span class="sxs-lookup"><span data-stu-id="bf3e2-137">The source code file can contain any of the following source code:</span></span><br /><br /> <span data-ttu-id="bf3e2-138">-A implementação do Gestor de autorização que substitui o Gerenciador de autorização de predefinição.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-138">-   The Authorization manager implementation that overrides the default authorization manager.</span></span> <span data-ttu-id="bf3e2-139">(Isso poderia também ser fornecido compilado num assembly.)</span><span class="sxs-lookup"><span data-stu-id="bf3e2-139">(This could also be supplied compiled into an assembly.)</span></span><br /><span data-ttu-id="bf3e2-140">-Declarações de atributo informativa assemblagem: como AssemblyCompanyAttribute, AssemblyCopyrightAttribute, AssemblyFileVersionAttribute, AssemblyInformationalVersionAttribute, AssemblyProductAttribute, e AssemblyTrademarkAttribute.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-140">-   Assembly informational attribute declarations: such as the AssemblyCompanyAttribute, AssemblyCopyrightAttribute, AssemblyFileVersionAttribute, AssemblyInformationalVersionAttribute, AssemblyProductAttribute, and AssemblyTrademarkAttribute.</span></span>|
|<span data-ttu-id="bf3e2-141">-authorizationmanager authorizationManagerType</span><span class="sxs-lookup"><span data-stu-id="bf3e2-141">-authorizationmanager authorizationManagerType</span></span>|<span data-ttu-id="bf3e2-142">O tipo que contém a implementação do Gestor de autorização.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-142">The type that contains the authorization manager implementation.</span></span> <span data-ttu-id="bf3e2-143">Isso pode ser definido no código-fonte ou compilado num assembly (especificado pelo `reference` parâmetro).</span><span class="sxs-lookup"><span data-stu-id="bf3e2-143">This can be defined in source code, or compiled into an assembly (specified by the `reference` parameter).</span></span> <span data-ttu-id="bf3e2-144">Se este parâmetro não for especificado, é utilizado o Gestor de segurança padrão.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-144">If this parameter is not specified, the default security manager is used.</span></span> <span data-ttu-id="bf3e2-145">O valor deve ser o nome de tipo completo, incluindo espaços de nomes.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-145">The value should be the full type name, including namespaces.</span></span>|
|<span data-ttu-id="bf3e2-146">-win32icon i.ico</span><span class="sxs-lookup"><span data-stu-id="bf3e2-146">-win32icon i.ico</span></span>|<span data-ttu-id="bf3e2-147">O ícone para o ficheiro de .exe para o shell.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-147">The icon for the .exe file for the shell.</span></span> <span data-ttu-id="bf3e2-148">Se não for especificado, o shell tem o ícone que o compilador c# inclui (se houver).</span><span class="sxs-lookup"><span data-stu-id="bf3e2-148">If not specified, then the shell will have the icon that the c# compiler includes (if any).</span></span>|
|<span data-ttu-id="bf3e2-149">-initscript p.ps1</span><span class="sxs-lookup"><span data-stu-id="bf3e2-149">-initscript p.ps1</span></span>|<span data-ttu-id="bf3e2-150">O perfil de inicialização para o shell.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-150">The startup profile for the shell.</span></span> <span data-ttu-id="bf3e2-151">O ficheiro está incluído "como-é"; sem verificação de validade é feito pelo Make-Shell.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-151">The file is included "as-is"; no validity checking is done by Make-Shell.</span></span>|
|<span data-ttu-id="bf3e2-152">-builtinscript s1.ps1[,s2.ps1,...]</span><span class="sxs-lookup"><span data-stu-id="bf3e2-152">-builtinscript s1.ps1[,s2.ps1,...]</span></span>|<span data-ttu-id="bf3e2-153">Uma lista de scripts incorporadas para o shell.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-153">A list of built-in scripts for the shell.</span></span> <span data-ttu-id="bf3e2-154">Estes scripts são detetados antes de scripts no caminho, e seu conteúdo não pode ser alterado depois do shell é criado.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-154">These scripts are discovered before scripts in the path, and their contents cannot be changed once the shell is built.</span></span><br /><br /> <span data-ttu-id="bf3e2-155">Os ficheiros estão incluídos "como-é"; sem verificação de validade é feito pelo Make-Shell.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-155">The files are included "as-is"; no validity checking is done by Make-Shell.</span></span>|
|<span data-ttu-id="bf3e2-156">-resourcefile.txt de recursos</span><span class="sxs-lookup"><span data-stu-id="bf3e2-156">-resource resourcefile.txt</span></span>|<span data-ttu-id="bf3e2-157">O ficheiro. txt que contém recursos de ajuda e faixa para o shell.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-157">The .txt file containing help and banner resources for the shell.</span></span> <span data-ttu-id="bf3e2-158">O primeiro recurso é o nome ShellHelp e contém o texto apresentado se o shell é invocado com o `help` parâmetro.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-158">The first resource is named ShellHelp, and contains the text displayed if the shell is invoked with the `help` parameter.</span></span> <span data-ttu-id="bf3e2-159">O segundo recurso com o nome ShellBanner e contém o texto e as informações de copyright apresentado quando o shell é iniciado no modo interativo.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-159">The second resource is named ShellBanner, and it contains the text and copyright information displayed when the shell is launched in interactive mode.</span></span><br /><br /> <span data-ttu-id="bf3e2-160">Se este parâmetro não for fornecido, ou estes recursos não estão presentes, em seguida, uma faixa de Ajuda genérica e são utilizados.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-160">If this parameter is not provided, or these resources are not present, then a generic help and banner are used.</span></span>|
|<span data-ttu-id="bf3e2-161">-cscflags cscFlags</span><span class="sxs-lookup"><span data-stu-id="bf3e2-161">-cscflags cscFlags</span></span>|<span data-ttu-id="bf3e2-162">Sinalizadores que devem ser passados para o C# compilador (csc.exe).</span><span class="sxs-lookup"><span data-stu-id="bf3e2-162">Flags that should be passed to the C# compiler (csc.exe).</span></span> <span data-ttu-id="bf3e2-163">Estes são transmitidos através de inalterado.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-163">These are passed through unchanged.</span></span> <span data-ttu-id="bf3e2-164">Se este parâmetro incluir espaços, devem ser rodeado de aspas duplas.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-164">If this parameter includes spaces, it should be surrounded in double-quotes.</span></span>|
|<span data-ttu-id="bf3e2-165">-?</span><span class="sxs-lookup"><span data-stu-id="bf3e2-165">-?</span></span><br /><br /> <span data-ttu-id="bf3e2-166">-Ajuda</span><span class="sxs-lookup"><span data-stu-id="bf3e2-166">-help</span></span>|<span data-ttu-id="bf3e2-167">Apresenta a mensagem de direitos autorais e opções de linha de comandos da Shell de marca.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-167">Displays the copyright message and Make-Shell command line options.</span></span>|
|<span data-ttu-id="bf3e2-168">-verbose</span><span class="sxs-lookup"><span data-stu-id="bf3e2-168">-verbose</span></span>|<span data-ttu-id="bf3e2-169">Mostra informações detalhadas, enquanto o shell está a ser criado.</span><span class="sxs-lookup"><span data-stu-id="bf3e2-169">Displays detailed information while the shell is being created.</span></span>|

## <a name="see-also"></a><span data-ttu-id="bf3e2-170">Consulte Também</span><span class="sxs-lookup"><span data-stu-id="bf3e2-170">See Also</span></span>

[<span data-ttu-id="bf3e2-171">Guia do programador do Windows PowerShell</span><span class="sxs-lookup"><span data-stu-id="bf3e2-171">Windows PowerShell Programmer's Guide</span></span>](./windows-powershell-programmer-s-guide.md)

[<span data-ttu-id="bf3e2-172">SDK do Windows PowerShell</span><span class="sxs-lookup"><span data-stu-id="bf3e2-172">Windows PowerShell SDK</span></span>](../windows-powershell-reference.md)